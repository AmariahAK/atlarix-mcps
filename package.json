{
  "name": "atlarix-mcps",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "sync:dry-run": "node scripts/sync.mjs --dry-run"
  }
}
